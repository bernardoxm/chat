import 'package:flutter/material.dart';

class NewMessages extends StatelessWidget {
  const NewMessages({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(child: Text('Nova Mensagem'),);
  }
}